# Notebooks

Use this directory for visual exploration and experiments. Keep final exercise
implementations in `exercises/` so they remain easy to test, review, and reuse.

Notebook tooling is not installed by default. Add it only when a problem
benefits from interactive plots or symbolic exploration.
